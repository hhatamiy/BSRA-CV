# Example training config, loaded via shared.config.load_config in train.py.
# Copy this to a new file per experiment rather than editing it in place.

model: yolov8n.pt
dataset: ../../data-team/datasets/ball/data.yaml
epochs: 100
imgsz: 640
batch: 16
